import { Perms, PermissionsType } from '../models/Permissions.ts'


export default class Bitield {

    // flags: {
    //     [name: string]: number
    // }
    // bitfield: number

    constructor (flags: { [name: string]: number }, bits: any) {

        // this.bitfield = Bitield.

    }

}